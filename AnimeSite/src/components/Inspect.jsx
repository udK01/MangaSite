export default function Inspect({ manga }) {
  return (
    <section className="w-[826px] h-auto bg-quaternary rounded-sm font-poppins">
      <h2 className="text-white">{manga.mangaTitle}</h2>
    </section>
  );
}
